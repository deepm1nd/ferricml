[package]
name = "example-mnist-tutorial"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
ferricml = { path = "../../crates/ferricml" }
ferric-core = { path = "../../crates/ferric-core" }
ferric-nn = { path = "../../crates/ferric-nn" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
